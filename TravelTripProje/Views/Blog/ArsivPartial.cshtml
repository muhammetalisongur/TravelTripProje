@using TravelTripProje.Models.Entity
@model List<Blog>
@foreach (var item in Model.Take(10))
{
    <ul>
        <li><a href="/Blog/BlogDetay/@item.ID">@item.Tarih.ToString("dd MMM yyyy")</a></li>

    </ul>
}